/**
 * サービス名などの表示設定
 * ────────────────────────────────────────────────
 * ★ 画面に出すサービス名は、ここだけで定義する。
 *   各画面に直接書かないこと。名称が決まったとき、この1か所を直せば
 *   全画面に反映される。
 *
 * ここに無い「スタジオ名」は別物。あちらはテナントごとの値で、
 * brand_settings に入っている（設計書 12章）。
 *   サービス名   … Studio Flow（この SaaS の名前。全テナント共通）
 *   スタジオ名   … 契約したスタジオの名前（テナントごとに違う）
 *
 * ★ メールの差出人名はここでは決まらない。
 *   Supabase の Edge Function は別の実行環境（Deno）で動くため、
 *   このファイルを読めない。差出人名は Supabase のシークレット
 *   MAIL_FROM の表示名部分が使われる。
 *
 *     npx supabase secrets set --project-ref <ref> \
 *       MAIL_FROM="新しいサービス名 <noreply@ドメイン>"
 *
 *   スタジオ名が設定されている場合は、そちらが差出人名になる（設計書 11章）。
 */

/** この SaaS の名前。仮称。決まったらここを書き換える */
export const APP_NAME = "Studio Flow";

/** サービスの一行説明。ログイン前の画面とブラウザのタブに出る */
export const APP_DESCRIPTION = "ダンススタジオ向けのスクール運営管理サービス";

/**
 * 保護者・生徒向けの画面に出す控えめな表記（設計書 12章）。
 * あちらではスタジオのロゴと名前が主表示で、サービス名は補助にとどめる。
 */
export const POWERED_BY = `Powered by ${APP_NAME}`;
